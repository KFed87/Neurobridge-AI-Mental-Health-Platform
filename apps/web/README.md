# Web Application



This directory will contain the front-end application for the Neurobridge platform. Move your existing React/Vite code into this folder. Keep each app under its own subdirectory inside `apps/`.
