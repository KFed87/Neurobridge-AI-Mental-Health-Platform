# Migrations

This directory contains database migration scripts used to evolve the Neurobridge platform's database schema over time. Use this folder to store SQL scripts or migration tool configurations to apply schema changes in a controlled and consistent manner across all environments.
